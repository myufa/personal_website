import * as React from 'react';
/** @jsx jsx */
import { css, jsx } from '@emotion/core';
import { withRouter } from 'react-router-dom';

const EdList: React.FC = () => {
    return (
        <p>It's a me, a Yufa</p>
    )
}


export default withRouter(EdList);